import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eletronic.prescription',
  appName: 'eletronic-prescription',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
